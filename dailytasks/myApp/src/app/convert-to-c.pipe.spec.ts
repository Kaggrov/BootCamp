import { ConvertToCPipe } from './convert-to-c.pipe';

describe('ConvertToCPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToCPipe();
    expect(pipe).toBeTruthy();
  });
});
