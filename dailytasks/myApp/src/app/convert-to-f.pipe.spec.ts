import { ConvertToFPipe } from './convert-to-f.pipe';

describe('ConvertToFPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToFPipe();
    expect(pipe).toBeTruthy();
  });
});
